function setup() {
    let canvas=createCanvas(windowWidth* 0.60, 800);
    canvas.parent('game-container');
  }
  
  // function windowResized() {
  //   resizeCanvas(windowWidth, windowHeight);
  // }
  
  function draw() {
    background(0);
  }